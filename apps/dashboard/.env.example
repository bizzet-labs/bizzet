# Neon の接続文字列。未設定ならローカル開発の DB（compose.yaml）につなぐ
DATABASE_URL=

# Better Auth がセッションの署名に使う秘密鍵。本番では必須（`openssl rand -base64 32` などで生成）
BETTER_AUTH_SECRET=

# ダッシュボードの公開 URL（例 https://admin.bizzet.xyz）。未設定ならリクエストから決める
BETTER_AUTH_URL=
