---
sidebar_position: 14
sidebar_label: Screens
---

# 画面一覧

ベータ版の画面を、ID を付けて一覧にします。画面は、ウォレットとダッシュボードの2つに分けます。どの画面も、ロールを付与されたグループとその配下のデータだけを表示します。

## ウォレット

メンバーが自分のウォレットを使うための画面です。

| ID | 画面 | 目的 | 使えるロール | 機能 |
| --- | --- | --- | --- | --- |
| WS-01 | ログイン | パスキーを登録し、ウォレットの画面に入る | すべて | W-01 |
| WS-02 | ホーム | 自分のグループの残高を見る | すべて | W-02 |
| WS-03 | 送金の作成 | 宛先・金額・通貨を入れて、出金を作る | Owner、Approver | W-03 |
| WS-04 | 承認待ちの一覧 | 自分が署名していない出金を見る | Owner、Approver | W-04 |
| WS-05 | 承認 | 1件の出金の内容を確かめ、パスキーで署名する | Owner、Approver | W-04 |

### WS-01 ログイン

- 初めてのメンバーは、端末でパスキーを作り、パスキーの署名者を登録する
- 2回目からは、パスキーでログインする

### WS-02 ホーム

- 自分のグループの残高を、通貨（JPYC・USDC）ごとに出す
- Owner と Approver には、承認待ちの出金の件数を出し、WS-04 へ移れるようにする

### WS-03 送金の作成

- 宛先・金額・通貨を入れて、出金を作る
- 作った人のパスキーの署名を、1人目の承認として付ける
- 作った出金は、もう1人の承認を待つ状態になる

### WS-04 承認待ちの一覧

- 自分がまだ署名していない出金を、新しい順に並べる
- 各行に、金額・宛先・作成者・作成日時を出す

### WS-05 承認

- 金額・宛先・作成者・すでに承認した人を出す
- パスキーで署名すると、2人目の承認がそろい、出金が実行される

## ダッシュボード

ロールと所属グループに合わせて、残高・出金・メンバーを見るための画面です。

| ID | 画面 | 目的 | 使えるロール | 機能 |
| --- | --- | --- | --- | --- |
| DS-01 | ログイン | ダッシュボードに入る | すべて | D-01 |
| DS-02 | ホーム | 見られるグループの残高と、承認待ちの出金の件数を一目で見る | すべて | D-02、D-06 |
| DS-03 | グループ詳細 | 1つのグループの残高と入金の履歴を見る | すべて | D-02、D-03 |
| DS-04 | 出金一覧 | 承認待ちと実行済みの出金を見る | Owner、Approver | D-04 |
| DS-05 | 出金詳細 | 1件の出金の内容と承認の状況を見る | Owner、Approver | D-04 |
| DS-06 | メンバー一覧 | グループごとのメンバーとロールを見る | Owner | D-05 |
| DS-07 | メンバーの追加 | メンバーを招待し、グループとロールを割り当てる | Owner | D-05 |
| DS-08 | ブリッジの状況 | 別のチェーンから本部のウォレットへ移動中の売上を見る | すべて | D-06 |

### DS-01 ログイン

仮置き：ログイン方式は、D-01 を決めた時点で更新します。

### DS-02 ホーム

- 見られるグループの残高を、通貨（JPYC・USDC）ごとに並べる
- Owner と Approver には、承認待ちの出金の件数を出し、DS-04 へ移れるようにする
- ブリッジ待ちの売上がある場合は、その合計を残高と分けて出す

### DS-03 グループ詳細

- グループの残高を、通貨ごとに出す
- 入金の履歴を、日時・金額・通貨・支払われたチェーンとともに新しい順に並べる

### DS-04 出金一覧

- 出金を「承認待ち」「実行済み」に分けて並べる
- 各行に、金額・宛先・作成日時・承認の数（2人中何人か）を出す

### DS-05 出金詳細

- 金額・宛先・作成者・承認した人を出す
- 承認の署名はウォレットの画面で行うため、この画面では承認の状況を見るだけにし、WS-05 へ移れるようにする

### DS-06 メンバー一覧

- グループごとに、メンバーの名前・役職・ロールを並べる

### DS-07 メンバーの追加

- メンバーを招待し、所属するグループとロール（Owner・Approver・Viewer）を選ぶ
- Owner と Approver を割り当てた場合は、そのメンバーのパスキーの署名者を、グループの Safe のオーナーに加える

### DS-08 ブリッジの状況

- 別のチェーンで着金し、本部のウォレットへ移動中の売上を並べる
- 各行に、送り元のチェーン・金額・着金日時・到着の見込みを出す
