# ドキュメント作成ルール

**共通の狙い**：1つの変更の影響範囲を1箇所に閉じ込め、修正漏れによる矛盾を防ぐこと。以下のルールの解釈が分かれた場合は、この狙いに沿う側を選ぶ。

## 1. 文書の構成

### 抽象から具体へ並べる

- 文書は「何のための文書か → 全体の考え方 → 個々の詳細 → 決まっていないこと」の順に並べる。読者が途中で読むのをやめても、そこまでの理解が成り立つようにするため。
- 冒頭で文書の目的と扱う範囲を示し、扱わない範囲も明示する。
- 同じ階層に並ぶセクションは、内部の構成を揃える。読者が同じ観点で比較できるため。

### 1セクションは1つのことだけを説明する

- 1つのセクションが負う責務は1つに限る。そのセクションの説明対象から外れる話題が出てきたら、同じセクション内で掘り下げず、別セクションとして切り出す。
- 以下に当てはまったら分割する合図とみなす。
  - 見出しが「AとB」「A・B」のように複数の対象を並べないと内容を言い表せない。
  - 見出しが示していない話題の説明が本文に混ざっている。
  - 一覧や流れを示す記述の途中に、個別項目の詳しい説明が割り込んでいる。
- 分割の結果セクション数が増えることは許容する。1セクションが長くなるより、見出しを追うだけで文書の内容を把握できる方を優先する。
- 一覧を示すセクションの側には個別の詳細を残さず、切り出し先のセクション名で受ける。
- 理由：1つのセクションに複数の説明が混ざると、内容を変更するときに直すべき箇所を特定しづらくなり、読者も見出しから目的の記述に辿り着けないため。

### 文書を自己完結させる

- 他のファイルを読者に参照させる記載をしない。リンクやファイル名はもちろん、「詳細は別資料を参照」のように文書名・資料名を挙げて参照を促す文言も書かない。
- 他の文書にある前提・数値・経緯が必要な場合は、参照を促すのではなく、その文書を確認した上で必要な情報をこのドキュメント自身に書き下す。
- 出典・根拠を示す場合も、参照先のファイルを指すのではなく、出典そのもの（公表元・公表時期・調査時点など）を文書内に書く。
- 同一ドキュメント内の他セクションへの参照は制限しない。他ファイルへの参照の代わりに、こちらを使う。
- 理由：ドキュメントは単体で共有・閲覧されることが多く、他文書への参照は読者に別ファイルを探させる手間を強い、参照先が単体で読めない場合に文書の自己完結性を損なうため。またファイルの移動・改名のたびに参照記述を直す必要が生じるのを防ぐため。

## 2. 情報の置き場所

### 同じ具体は1箇所にのみ置く

- 具体的な数値・固有名詞・仕組みの詳細は、それを説明する責務を持つ1箇所にのみ置く。セクション間だけでなく、同一セクション内（例：説明文とその直後のまとめ文）でも重複させない。
- 概要・サマリーにあたるセクションは抽象的な説明にとどめ、詳細セクションで扱うべき具体を先取りしない。
- まとめ文では数値を再掲せず、そこから導かれる結果（比率・傾向・判断）のみを述べる。
- **例外**：文書全体の前提となる値・条件は、前提を示すセクションに1箇所だけ置いてよい。その場合、他のセクションは値を再掲せず、その前提を指す表現で受ける。値の実体が2箇所に増えないようにするため。
- 理由：同じ具体を複数箇所に書くと、値を変更するたびに全箇所を洗い出す必要が生じ、修正漏れによる矛盾が発生しやすくなるため。

### 未確定のものは未確定と明記する

- 根拠が確定していない数値・仕様・判断は、仮置きである旨と、何が確定すれば更新するかを、その記述の冒頭に明記する。
- 理由：仮置きの値が確定値として独り歩きすると、合意していない前提のまま意思決定が進むため。

## 3. 文章表現

### 独立した短文を並べない

- 同じ文末表現で終わる短い文が2つ以上続き、それらに因果関係や補足関係がある場合は、接続表現（〜ことで、〜のため等）で1文にまとめる。内容につながりのない別々の論点であれば、分けたままでよい。
- 対象は本文の地の文。表のセルと箇条書きの各項目は、独立した記載として扱うため対象外とする。
- 理由：短文の羅列は文章のつながりが失われ、実際の情報量以上に読み手に負荷を感じさせるため。

### 表のセルは1文にとどめる

- 表のセルは原則1文、おおむね60字以内に収める。2文以上必要になったら表に収める内容ではない合図とみなし、列を分けるか、表の外の本文に出すか、別セクションに切り出す。
- セルには、列見出しが問う観点への答えだけを書く。成立条件・補足・別の論点を混ぜない。
- 同じ行の他の列にすでにある情報を、セル内で繰り返さない。
- 1つの列に2つの観点を兼ねさせない。列見出しが「AとB」になったら列を分ける。
- セルは文ではなく名詞句・体言止めで書く。主語と観点は列見出しが与えるため、セル内で繰り返さない。
- セルの中に箇条書きを入れない。箇条書きが必要な粒度なら、その行は独立したセクションにすべき内容とみなす。
- 理由：表は行方向に比較するためのもので、セルが長文になると読者が1行ずつ読み下すことになり、表にした意味が失われるため。

## 4. 変更時の手順

### 具体を変更したら関連箇所を見直す

- 具体的な数値・仕様・固有名詞を変更した場合は、同一ドキュメント内に矛盾や重複した具体が残っていないか確認する。
  - 概要セクションに詳細な具体が紛れ込んでいれば抽象的な記載に直す。
  - セクション間で数値・内容が食い違っていれば整合させる。
- 誤字修正など具体の変更を伴わない軽微な修正では、この見直しは不要。

# 会話のルール

- ユーザーへの応答（チャットの出力）は、すべて日本語で書く。途中経過の説明、作業の報告、質問も日本語にする。
- コード、コマンド、ファイル名、固有名詞、エラーメッセージの引用は原文のままでよい。
- サブエージェントの報告が英語で届いた場合も、ユーザーには日本語に直して伝える。
